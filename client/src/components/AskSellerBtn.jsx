const AskSellerBtn = ({ seller }) => {
  function onClick() {
    // open the modal
  }

  return (
    <button className="ask-seller-btn" onClick={onClick}>
      Ask a question
    </button>
  );
}

export default AskSellerBtn;
