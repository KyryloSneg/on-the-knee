import DeviceList from "./DeviceList";

const DeviceSection = () => {
  return (
    <main>
      <DeviceList />
      {/* pagination btn component / dynamical pagination */}
    </main>
  );
}

export default DeviceSection;
